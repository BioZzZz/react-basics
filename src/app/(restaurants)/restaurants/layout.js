import { RestaurantsSubPagesLayout } from "../../../components/pages/restaurantsSubPagesLayout";

const RestaurantsLayout = ({ children }) => {
  return <RestaurantsSubPagesLayout children={children} />;
};

export default RestaurantsLayout;
